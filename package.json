{
  "name": "co2-plus",
  "private": true,
  
  "scripts": {
    "dev": "concurrently -n ASSET,AUTH,NOTIFY,ADMIN,ORG,USER -c green,blue,yellow,magenta,cyan,white \"npm --prefix backend/assetService run dev\" \"npm --prefix backend/authentication run dev\" \"npm --prefix backend/notification-service run dev\" \"npm --prefix frontend/Admin run dev\" \"npm --prefix frontend/Organization run dev\" \"npm --prefix frontend/User run dev\""
  }

,
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
